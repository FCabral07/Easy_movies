import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
  logo: {
    width: 300,
    height: 300,
  },
  container: {
    alignItems: 'center',
  }
  
})

export default styles